.venv/
__pycache__/
*.egg-info/
.pytest_cache/
*.pyc
dist/
build/

# L3 test-only secrets (never commit) — config/bot/app tokens, channel & user ids
.env.test
.env.test.*
*.env.test

# Local-only dev/agent artifacts — kept local, not part of the end-user project.
# Removed from history; ignored so they are never re-tracked.
docs/
.claude/
.agents/
.agent/
CLAUDE.md
AGENTS.md
GEMINI.md
.cursor/
.windsurf/
.mcp.json
.aider*
